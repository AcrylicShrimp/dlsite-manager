# Dependency refresh — 2026-09-09

## Status

Implementation and local automated validation complete. No application version bump.

- [x] Refresh all direct Rust/npm requirements and compatible transitive dependencies.
- [x] Move ZIP to 8.6.0 and SQLx to stable 0.9.0; retain existing storage migrations.
- [x] Migrate Vite/Svelte plugin/Vitest and the TypeScript checker to their new APIs.
- [x] Align pnpm 12.3.4 across local setup and CI; update available GitHub Actions.
- [x] Validate Rust checks/tests/clippy, frontend types/tests/build, Storybook,
      frozen dependency installation, and the macOS native release build.
- [x] Exercise Deflate and UTF-8 archive names with an independently generated fixture.

## Follow-up constraints

- [ ] Remove the TypeScript 6 compiler-API dependency once SvelteKit/svelte-check and
      Storybook support TypeScript 7's native API. TypeScript 7.0.2 already runs the
      checker through `@typescript/native` and `--tsgo`; the older API package is
      intentional, not an overlooked update. See the frontend workbench design.
- [ ] Recheck SvelteKit's transitive `cookie ^0.6.0` when its dependency range changes.
      `pnpm audit` reports one low-severity development dependency advisory,
      [GHSA-pxg6-pf52-xh8x](https://github.com/advisories/GHSA-pxg6-pf52-xh8x).
      SvelteKit 2.70.3 still requires this range
      ([upstream tracking](https://github.com/sveltejs/kit/issues/13089)). No forced
      override or audit suppression was added. This application packages a static SPA
      with no SvelteKit server; its DLsite authentication cookies are handled in Rust.
- [ ] Complete Windows/Linux builds and interactive native acceptance as part of the
      existing release-candidate checklist. Local validation covered macOS compilation,
      not cross-platform execution, signed installers/updaters, or live account flows.

See `docs/work-log.md` for commands and results.
