# TODO: Refactor frontend into reusable components

## Current Status
- State: Partially Taken
- Dropped Reason: 
- Last Status Update: 2026-08-12 18:54:06

## Context
The current Svelte frontend is concentrated in src/routes/+page.svelte, which is over 6,000 lines and mixes DTO types, Tauri command calls, formatting helpers, view state, page markup, dialogs, and CSS. The user wants reusable component sets and dedicated page components before further GUI growth.

## Why It Is Needed
The all-in-one page makes UI patterns hard to reuse consistently. Recent Library-specific layout/background fixes show that shared page templates and common components are missing.

## Completion Criteria
- [x] Shared frontend types, constants, and pure helper functions are moved out of `+page.svelte`.
- [x] Reusable shell/overlay components exist for app layout, navigation, workspace header, toasts, and dialogs.
- [x] Library UI is split into dedicated components without behavior changes.
- [ ] Downloads, Accounts, Activity, and Settings views are split into dedicated components without behavior changes.
- [ ] Tauri command wrappers and state orchestration are separated only after UI extraction proves the shape.
- [ ] Each slice passes `pnpm check`, `pnpm build`, and `git diff --check`.
- [ ] Refactor commits remain behavior-preserving and reviewable.

## Detailed Plan

### Target Structure

```text
.storybook/
  main.ts
  preview.ts
src/lib/
  api/
    tauri.ts
  components/
    AppShell.svelte
    SidebarNav.svelte
    WorkspaceHeader.svelte
    Panel.svelte
    EmptyState.svelte
    ToastStack.svelte
    ConfirmationDialog.svelte
    BulkDownloadDialog.svelte
  features/
    library/
      LibraryView.svelte
      LibraryControls.svelte
      LibraryFilters.svelte
      ProductCard.svelte
      ProductDetailDialog.svelte
      ProductImagePreview.svelte
      ProductActionMenu.svelte
    downloads/
      DownloadsView.svelte
      DownloadQueueRow.svelte
    accounts/
      AccountsView.svelte
      AccountSourceRow.svelte
      AccountEditor.svelte
    activity/
      ActivityView.svelte
      JobList.svelte
      AuditLogList.svelte
    settings/
      SettingsView.svelte
  model/
    types.ts
    constants.ts
  styles/
    theme.css
  utils/
    accounts.ts
    format.ts
    jobs.ts
    products.ts
src/stories/
  *.stories.ts
  harnesses/
```

Storybook is the component workbench for this refactor. Display components should receive plain
props and callbacks so their important states can render without the Tauri runtime. Shared visual
tokens live in `src/lib/styles/theme.css` and are imported by both the app and Storybook.

### Phase 1: Pure Type And Helper Extraction

Move behavior-free TypeScript pieces first:

- DTO and view types into `src/lib/model/types.ts`.
- filter constants and DLsite product-type metadata into `src/lib/model/constants.ts`.
- date, bytes, nullable string, app-info, and error formatting helpers into `src/lib/utils/format.ts`.
- product type, age label/tone, credit grouping, and detail helper functions into `src/lib/utils/products.ts`.
- job labels, queue summaries, progress percentages, and job output parsing into `src/lib/utils/jobs.ts`.
- account label/status/count helpers into `src/lib/utils/accounts.ts`.

Keep all Svelte state, command calls, event listeners, and markup in `+page.svelte` during this phase.

### Phase 2: Shared Shell And Overlay Components

Extract reusable UI that has clear boundaries and low coupling:

- `SidebarNav.svelte` for view navigation.
- `WorkspaceHeader.svelte` for page title/eyebrow.
- `ToastStack.svelte` for toast rendering/dismissal.
- `ConfirmationDialog.svelte` for generic confirmation UI.
- `BulkDownloadDialog.svelte` for bulk preview/notice UI.

`+page.svelte` should still own the dialog/toast state and pass data plus callbacks into these components.

### Phase 3: Library Component Split

Split the Library view without changing behavior:

- `LibraryView.svelte` receives products, filter state, jobs, facets, and callbacks.
- `LibraryControls.svelte` renders the sticky search/action shell.
- `LibraryFilters.svelte` renders expanded filters as normal page content.
- `ProductCard.svelte` renders one product row/card.
- `ProductActionMenu.svelte`, `ProductDetailDialog.svelte`, and `ProductImagePreview.svelte` render existing overlays.

Keep controller logic in `+page.svelte` until component boundaries are stable.

### Phase 4: Secondary Page Components

Extract the remaining pages:

- `DownloadsView.svelte` and `DownloadQueueRow.svelte`.
- `AccountsView.svelte`, `AccountSourceRow.svelte`, and `AccountEditor.svelte`.
- `ActivityView.svelte`, `JobList.svelte`, and `AuditLogList.svelte`.
- `SettingsView.svelte`.

Each page component should receive plain props and callbacks. Avoid adding global stores in this phase.

### Phase 5: Command And State Orchestration Cleanup

Only after UI extraction is stable:

- Add typed Tauri wrappers in `src/lib/api/tauri.ts`.
- Consider small controller modules for Library, Accounts, Jobs, Audit, and Settings.
- Keep command wrappers thin and avoid hiding long-running job behavior behind opaque abstractions.

### Validation And Commit Policy

- Run `pnpm check`, `pnpm build`, and `git diff --check` after every slice.
- Run `pnpm build-storybook` for component or shared-theme changes and visually inspect affected
  stories at desktop and relevant narrow breakpoints.
- Use `cargo check -p dlsite-manager` only when Tauri command DTOs or Rust-facing contracts change.
- Commit each phase or sub-phase separately.
- Do not mix visual redesign or new functionality into refactor commits.

## Activity Log
- [2026-05-11 23:43:51] [USER] User clarified that the frontend refactor plan should be captured in a TODO file before implementation.
- [2026-05-11 23:43:51] [ACTION] Created a TODO tracker for the frontend componentization/refactor plan.
- [2026-05-11 23:43:51] [RESULT] The refactor can now proceed through documented, reviewable slices.
- [2026-05-11 23:44:22] [ACTION] Filled the TODO with phased extraction targets, target file structure, validation policy, and commit policy.
- [2026-05-11 23:44:22] [RESULT] The frontend refactor now has an implementation-ready plan without changing application code.
- [2026-05-11 23:53:10] [ACTION] Started Phase 1 by extracting frontend DTO/view types into `src/lib/model/types.ts` and static URL/filter/product-type metadata into `src/lib/model/constants.ts`.
- [2026-05-11 23:53:10] [RESULT] `+page.svelte` now imports model data from reusable modules; pure helper extraction remains.
- [2026-05-11 23:57:58] [ACTION] Extracted formatting helpers into `src/lib/utils/format.ts`, including byte/date/status/detail/app-info/error formatting.
- [2026-05-11 23:57:58] [RESULT] `+page.svelte` keeps state and markup but no longer owns generic formatting helpers.
- [2026-05-12 00:01:53] [ACTION] Extracted product-domain helpers into `src/lib/utils/products.ts`, including product type metadata mapping, age labels/tooltips, credit rows, and detail tag filtering.
- [2026-05-12 00:01:53] [RESULT] Library and detail markup can reuse product helper logic from a dedicated utility module.
- [2026-05-12 00:04:48] [ACTION] Extracted pure account label/count helpers into `src/lib/utils/accounts.ts`.
- [2026-05-12 00:04:48] [RESULT] Job-aware account status remains in `+page.svelte` until job helper extraction defines the shared shape.
- [2026-05-12 00:12:12] [ACTION] Extracted pure job/audit/download-queue helper logic into `src/lib/utils/jobs.ts`.
- [2026-05-12 00:12:12] [RESULT] Stateful selectors that close over `jobs`, `accounts`, `products`, or `jobMessages` remain in `+page.svelte` for the later controller/component split.
- [2026-05-12 00:16:34] [ACTION] Started Phase 2 by extracting `ToastStack.svelte` with scoped toast rendering/styles.
- [2026-05-12 00:16:34] [RESULT] Toast state remains page-owned while notification UI is now reusable.
- [2026-05-12 00:20:40] [ACTION] Extracted the generic confirmation overlay into `ConfirmationDialog.svelte`.
- [2026-05-12 00:20:40] [RESULT] Destructive-operation confirmations now render through a reusable component while the page still owns dialog state.
- [2026-05-12 00:36:27] [ACTION] Added simple UI primitives `Button.svelte`, `TextInput.svelte`, and `Field.svelte`, then applied them to the Settings page only.
- [2026-05-12 00:36:27] [RESULT] Common controls now have dedicated component files without forcing local page-specific buttons into a broad customization system.
- [2026-08-12 16:11:22] [ACTION] Added a minimal Storybook 10 SvelteKit workbench with shared theme tokens, accessibility tooling, stories for the existing controls/feedback components, and a component-foundations gallery.
- [2026-08-12 16:11:22] [RESULT] Components can now be rendered and visually reviewed outside Tauri; desktop and 390px browser checks passed without horizontal overflow. Page and feature extraction remain.
- [2026-08-12 16:43:14] [ACTION] Extracted `AppShell.svelte`, `SidebarNav.svelte`, and `WorkspaceHeader.svelte`, moved view-title mapping into the shell, and added shell/header stories with interactive navigation fixtures.
- [2026-08-12 16:43:14] [RESULT] Phase 2 shell and overlay boundaries are complete. Browser QA caught and fixed a 390px grid minimum-width overflow while preserving access to all five views; Library and feature-page extraction remain.
- [2026-08-12 16:52:28] [ACTION] Started Phase 3 by extracting `features/library/ProductCard.svelte`, moving local-only helpers into the product utility module, and extending the shared Button with explicit mobile-width and expanded-state props.
- [2026-08-12 16:52:28] [RESULT] Product cards now render from plain product/action props with page-owned side effects and four deterministic stories. Browser QA also fixed clipped owner/action rows below 980px; remaining Library controls, filters, overlays, and view extraction are still pending.
- [2026-08-12 17:00:25] [ACTION] Extracted the sticky search/action shell into `features/library/LibraryControls.svelte`, composed it from shared inputs/buttons, and added Ready, expanded-filter, and Busy stories.
- [2026-08-12 17:00:25] [RESULT] The route now owns Library query/job behavior while the controls component owns responsive presentation and accessible filter disclosure. Desktop and 390px Browser QA passed without overflow; Library filters, overlays, and full view extraction remain pending.
- [2026-08-12 17:08:09] [ACTION] Extracted sort, account, source, age, type, maker, and tri-state custom-tag presentation into `features/library/LibraryFilters.svelte` with default and active-selection stories.
- [2026-08-12 17:08:09] [RESULT] Filter state mutations and cache reloads remain route-owned behind explicit callbacks, while the reusable filter panel now owns semantic colors, wrapping, counts, truncation, and phone layout. Browser QA verified interactions and desktop/390px rendering without overflow; Library overlays and full view extraction remain pending.
- [2026-08-12 17:11:28] [ACTION] Extracted the status-dependent product overflow menu into `features/library/ProductActionMenu.svelte` with not-downloaded, downloaded, failed, and busy stories.
- [2026-08-12 17:11:28] [RESULT] The route still owns anchor-based positioning and download mutations; the menu now owns accessible menu markup, action availability, danger styling, focus treatment, and Escape intent. Desktop and 390px Browser QA passed; product detail/image overlays and full Library view extraction remain pending.
- [2026-08-12 17:15:35] [ACTION] Extracted the full-screen thumbnail overlay into `features/library/ProductImagePreview.svelte` with default and long-title stories.
- [2026-08-12 17:15:35] [RESULT] Product selection state remains route-owned while the preview owns dialog semantics, image containment, accessible alternative text, backdrop/close/Escape intent, and phone spacing. Desktop and 390px Browser QA passed; product detail and bulk-dialog extraction plus full Library view extraction remain pending.
- [2026-08-12 17:20:20] [ACTION] Extracted the bulk plan confirmation/notice overlay into `components/BulkDownloadDialog.svelte` with confirmation, partial-plan warning, and nothing-to-download stories.
- [2026-08-12 17:20:20] [RESULT] Phase 2 shared shell/overlay extraction is complete. Bulk planning and promise resolution remain page-owned, while the component owns summary formatting, dialog roles, warning/notice copy, and responsive action layout. Desktop and 390px Browser QA passed; Product Detail and the full Library view boundary remain pending.
- [2026-08-12 18:40:21] [ACTION] Extracted the full Product Detail modal into `features/library/ProductDetailDialog.svelte`, including identity, credits, dates, ownership, download state, and custom-tag presentation/editing controls.
- [2026-08-12 18:40:21] [RESULT] Detail loading, clipboard, external URL, preview selection, and tag persistence remain route-owned through callbacks. Downloaded/owned, local-only/sparse, and failed-download stories passed desktop and 390px Browser QA; only the final `LibraryView` composition boundary remains for Phase 3.
- [2026-08-12 18:46:46] [ACTION] Completed Phase 3 by extracting `features/library/LibraryView.svelte` as the composition boundary for controls, filters, pagination, loading/empty states, and product cards.
- [2026-08-12 18:46:46] [RESULT] The route now supplies query/job-derived labels, disabled states, and explicit callbacks while `LibraryView` owns the complete Library surface. Populated, filters-open, loading, and empty stories passed desktop and 390px Browser QA; Phase 4 secondary views are next.
- [2026-08-12 18:54:06] [ACTION] Started Phase 4 by extracting `features/downloads/DownloadsView.svelte` and `DownloadQueueRow.svelte` with active, loading, empty, cancelling, and failed Storybook coverage.
- [2026-08-12 18:54:06] [RESULT] Active-job selection and product-aware title/detail projection remain route-owned, while the Downloads components own queue summary, progress, cancellation presentation, and responsive layout. Desktop and 390px Browser QA passed without horizontal overflow; Accounts is the next secondary view.
