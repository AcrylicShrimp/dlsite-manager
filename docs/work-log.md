# Work Log

## 2026-05-10

- Created `docs/todos/2026-05-10.00.add-generic-background-job-system.md` to track the proposed generic background job system. The plan covers a `dm-jobs` crate, cooperative cancellation, snapshots, bounded logs, generic Tauri job commands/events, account-sync migration, frontend job state, observability rules, and later download/archive integration. Validation: documentation-only change; inspected the generated TODO content, no code validation run.
- Added `docs/designs/2026-05-10.00-background-job-system.md` and the first `crates/dm-jobs` implementation. `dm-jobs` now owns process-local job lifecycle state, snapshots, generic events, bounded logs, cooperative cancellation, and finished-job cleanup without depending on DLsite/storage/Tauri code. `dm-library` now reuses the shared cancellation token through a public re-export. Validation: `cargo fmt --all`, `cargo test -p dm-jobs -p dm-library`, `cargo test --workspace`, `cargo check --workspace`, and `git diff --check` passed. The workspace test run included env-enabled live tests.
- Wired account sync through the generic background job system. `src-tauri` now owns a `JobManager`, forwards `dm-job-event`, exposes job start/list/get/cancel/log/clear commands, and starts account sync jobs that call `dm-library` with job progress and cancellation. The Products UI now queues sync jobs, renders active/recent job activity, supports cancellation, and refreshes account/product cache views when sync jobs finish. Validation: `cargo fmt --all`, `cargo test --workspace`, `cargo check --workspace`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed. The workspace test run included env-enabled live tests.
- Fixed account sync handling for DLsite `content/works` subset responses. `dm-library` now treats missing work-detail rows as placeholder cached works keyed by the purchased work ID instead of failing the whole sync with `MissingWorkDetails`; sync reports include `missing_detail_count`, and Tauri job output/logs surface the count. Updated the product-list design note to record the placeholder behavior. Validation: `cargo fmt --all`, `cargo test -p dm-library -p dm-storage -p dm-jobs`, `cargo check --workspace`, `pnpm check`, and `git diff --check` passed.
- Added age-category filtering to the cached product list. `dm-storage` now supports optional filters for DLsite `all`, `r15`, and `r18` age categories, the Tauri list-products DTO forwards the filter, and the Products toolbar has an explicit `Any age` unfiltered state plus `All ages`, `R-15`, and `R-18` category filters. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.
- Split the first GUI into dedicated Products, Accounts, Activity, and Settings views while keeping the existing CSS styling approach. Products now uses the full workspace width for the cached library list, Accounts owns source/account management, and Activity owns background job status/cancellation. Updated the unified product-list design note to reflect the dedicated screens. Validation: `pnpm check`, `pnpm build`, `cargo check -p dlsite-manager`, `pnpm tauri dev`, and `git diff --check` passed. The first Tauri smoke attempt found a stale repo-local dev process holding port 1420; stopping it and rerunning succeeded.
- Converted the current CSS-only GUI to a dark default theme. The Svelte page now uses neutral dark surfaces, muted text, green-accented primary actions/focus states, dark form controls, and dark product/account/job rows. The design note records that Tailwind migration remains a later separate slice. Validation: `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.
- Moved Activity out of the main sidebar navigation group and pinned it to the bottom utility area of the menu. The product-list view is now labeled Library, and the Reload/Sync actions remain scoped to that Library page. The design note now records Library, Accounts, and Settings as the main group with Activity separated for job/status monitoring. Validation: `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.
- Removed the global workspace-header action button area so page controls are no longer detached from their content. Library now keeps Search, Reload, and Sync in the library toolbar; Accounts keeps New/Reload/Sync controls in account panels; Activity keeps Reload/Clear in its job panel; Settings keeps Reload in the settings panel. Updated the product-list design note to record local page controls. Validation: `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed. The Tauri smoke first found a stale repo-local dev process on port 1420; stopping it and rerunning succeeded.
- Captured the agreed Library product-card redesign in `docs/designs/2026-05-10.01-library-product-card.md`. The design keeps maker and source-credit rows together in one metadata block, groups v3 `raw_json.tags` credits by role (`CV`, `Illust`, `Scenario`, `Creator`, `Music`, `Other`), reserves chips for classification/status, and defers functional download/serial actions until the backend exposes those states. Validation: documentation-only design capture; no code validation run.
- Extended the Library product-card design with color semantics. The left belt and type chip are product-type only; audio/voice is yellow, video is pink, games are purple/violet, CG/manga/PDF/reading material is green, and unknown/other is gray. Age chips are separate dimmed green/yellow/red for all-ages/R-15/R-18. Validation: documentation-only design update; no code validation run.
- Implemented the first Library product-card redesign. `dm-storage` now derives grouped source-credit metadata from cached v3 `raw_json.tags`, Tauri forwards the groups to the frontend, and the Library list renders square thumbnails, product-type belts/chips, age chips, copyable work IDs, maker/credit rows with native hover tooltips, owner tags, and disabled placeholder row actions for the later download-status slice. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `cargo test --workspace`, `git diff --check`, and `pnpm tauri dev` smoke passed; the smoke was stopped after the app launched.
- Tightened the Library product-card metadata grid spacing. The maker/credit rows now use responsive CSS variables for metadata column gaps, label width, and label/value gaps so the grid compacts at narrower widths without changing the data contract. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed.
- Constrained the Library product-card metadata block width after wide-screen review showed the second credit column drifting too far right. The maker/source-credit grid now stays left-aligned with a responsive max width on large screens and falls back to full width on narrower layouts. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed.
- Replaced the inline page status line with a frontend toast stack. Success/info/error feedback now appears as fixed bottom-right popup notifications with auto-dismiss, manual dismiss, and alert semantics for errors; sync-all sequencing no longer depends on a rendered error string. Validation: `pnpm check`, `pnpm build`, `git diff --check`, and `pnpm tauri dev` smoke passed. The first smoke attempt found a stale repo-local dev process on port 1420; stopping it and rerunning succeeded.
- Replaced the toast close button text with an icon-only SVG close control while preserving the accessible dismiss label. Validation: `pnpm check` and `pnpm build` passed.
- Added explicit labels for Library product-card tag and ownership rows. Type/age chips now live under a `Tags` field, account-owner chips live under an `Owned by` field, and these labeled chip rows are vertically centered with a slightly wider shared label column for responsive layouts. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed.
- Constrained Library scrolling to the product list area and stabilized product-row layout. The app shell now uses a fixed viewport grid, the Library panel is a flex column, toolbar/count header stay fixed in the panel, the product table owns vertical scrolling, and product cards use equal row heights with clipped metadata so rows do not overlap when labeled fields grow. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Made product-card credit rendering deterministic. `dm-storage` now sorts credit tags, folds unknown `*_by` source-credit classes into `Other`, and the Library UI always renders the full known credit field set (`Maker`, `CV`, `Illust`, `Scenario`, `Creator`, `Music`, `Other`) with muted `-` placeholders for missing values. Updated the product-card design note to record the fixed field contract. Validation: `cargo test -p dm-storage`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Made Library product-card credit fields copyable. Non-missing credit rows are accessible buttons that copy the displayed value to the clipboard and emit the existing toast notification; missing `-` rows stay disabled. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Redesigned the Accounts screen as source management. The account list is now primary, with summary counts, selected-row styling, explicit enabled/disabled pills, sync/credential/last-sync metadata, and a secondary add/edit detail panel. Updated the unified product-list/account design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Reshaped account source rows into taller cards matching the account-management sketch. The enabled/disabled state is now a top-left toggle badge, identity sits as the left block, sync metadata renders as a right-side label/value stack, and row actions are limited to sync/cancel until account removal is designed. Updated the account design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Tightened account source rows after visual review showed the taller card layout felt sparse and disconnected. Account rows now use a compact shared grid with smaller type, a row-level enabled toggle, identity, three metadata cells, and a right-aligned sync action. Updated the account design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.

## 2026-05-09

- Captured the unified multi-account product-list design in `docs/designs/2026-05-09.00-unified-product-list-and-accounts.md`. The design treats DLsite accounts as ownership sources under one virtual library, keeps list rendering cache-backed, defers serial/session-sensitive storage, and sets the next implementation slice for account/work ownership storage plus a `dm-library` orchestration crate. Validation: documentation-only change; no code validation run.
- Added `crates/dm-credentials` for remembered account passwords outside SQLite. The crate defines credential references, a `CredentialStore` trait, a native OS credential-store implementation through `keyring-core` plus target-native store crates, an in-memory test implementation, and an optional env-gated native keychain test. Updated the unified product-list design and milestone TODO so account rows store only non-secret `credential_ref` values. Validation: `cargo fmt --all`, `cargo test -p dm-credentials`, `cargo tree -p dm-credentials --depth 2`, and `cargo check --workspace` passed.
- Added the unified product-list storage cache in `dm-storage`: migrations for `accounts`, `works`, `account_works`, and `sync_runs`; transactional repository APIs for account metadata, sync commits/failures, and cached list queries; and tests for account CRUD, rollback, duplicate ownership collapse, disabled-account filtering, stale ownership, failed sync records, search, and sort. Updated storage/product-list design notes and the milestone TODO. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, and `cargo check --workspace` passed.
- Added `crates/dm-library` as the first account/product-list orchestration layer. It wraps `dm-storage` and `dm-credentials` for account save/list/password-reference behavior, exposes cached product-list queries, defines cancellable/progress-emitting sync requests, maps `dm-api` purchases/works into storage cache rows, records sync success/failure/cancellation, and provides a `DlsiteSyncSource` adapter. `dm-storage` also gained explicit cancelled sync-run recording. Validation: `cargo fmt --all`, `cargo test -p dm-storage -p dm-library`, `cargo tree -p dm-library --depth 2`, `cargo check --workspace`, and `git diff --check` passed.
- Added the first account/product Tauri integration. `src-tauri` now initializes `dm-library` with native `dm-credentials` when available, falls back to in-memory credentials if native storage is unavailable, exposes thin settings/account/product/sync commands, and emits sync progress events. The Svelte page now defaults to Products, shows cached unified products, includes account source management, and keeps settings as a secondary view. Validation: `cargo check -p dlsite-manager`, `cargo test -p dm-storage -p dm-library`, `cargo check --workspace`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.

## 2026-05-07

- Created `docs/todos/2026-05-07.00.set-up-rewrite-milestones.md` to track the staged rewrite plan: DLsite API v3 crate, download/decompression crate boundary, SQLite storage crate, and final Tauri GUI integration. Validation: inspected generated TODO content; no code validation run because this was documentation-only planning.
- Captured the agreed first-slice API crate design in `docs/designs/2026-05-07.00-dlsite-api-v3-surface.md`. The design keeps `dm-api` v3-first, owns auth/session and authenticated byte streams, delays split/download manifest contracts until live behavior is observed, and requires env-gated live tests. Validation: documentation-only change; no code validation run.
- Added the initial `crates/dm-api` Rust crate and root Cargo workspace with resolver `3`. The crate now covers v3 login/session import/export, content count, sales, works lookup with auto chunking, conservative download redirect resolution, authenticated direct download streams, a raw download probe, env-gated live tests, and `crates/dm-api/.env.example`. Live DTO handling was adjusted for observed nullable `regist_date`, `genre_ids`, and `tags`. Added `/target` to `.gitignore`. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo test --workspace` passed.
- Updated `docs/todos/2026-05-07.00.set-up-rewrite-milestones.md` to `Partially Taken`, marking the API env/test harness criterion complete while leaving full download API discovery, download/decompression, storage, and GUI milestones open. Validation: documentation-only TODO update after the workspace tests had passed.
- Updated `dm-api` to model `content/count` metadata: `production`, `page_limit`, and `concurrency`. The client now caches `page_limit` as the `content/works` batch limit during session validation and explicit count calls, while retaining header/body batch-limit fallback detection. Validation: `cargo fmt --all` and `cargo test -p dm-api` passed, including env-enabled live tests.
- Strengthened `dm-api` download and live-test coverage using pinned direct, split, and serial-required work IDs. Added `DownloadProbe`, explicit byte ranges for safe one-byte stream checks, manual redirect following for direct download streams, raw GET probing for redirect pages, and diagnostics for rejected live `content/works` chunks. Observed that split redirects can use `/home/download/split`, direct stream URLs can redirect again before the archive response, and `content/works` may return a subset of requested IDs. Fixed one malformed control sequence in the local ignored `.env` batch fixture. Validation: `cargo fmt --all` and `cargo test -p dm-api` passed with env-enabled live tests and the 1223-ID batch fixture.
- Probed split and serial-required follow-up pages and added typed parsing helpers in `dm-api`. Split pages now parse numbered part stream requests from `/home/download/=/number/{N}/product_id/{WORK_ID}.html`; serial-required pages now parse the normal direct download link from `/home/download/=/product_id/{WORK_ID}.html`. Added `crates/dm-api/examples/probe_download.rs` as a sanitized live probe tool. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download` passed.
- Added serial-number extraction for serial-required pages. `dm-api` now parses the observed `シリアル番号` table into `SerialNumber` label/value pairs and redacts serial values in `Debug`; the sanitized probe example reports counts and labels without printing serial values. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download | rg 'case=serial|serial numbers=|serial redacted values=|serial download url='` passed.
- Added unified `DownloadPlan` generation to `dm-api`. The plan normalizes direct, split, and serial-required downloads into `DownloadFile` entries plus optional serial-number metadata; serial-required works use the same `Direct` file kind as normal direct works. Optional serial-page probing treats an observed HTTP 500 for non-serial works as no serial page. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download | rg 'case=|download plan:|plan file|serial labels=|serial values='` passed.
- Strengthened download proof around the unified `DownloadPlan` surface. Live tests now open every planned file as an authenticated first-byte ranged stream and assert that bytes are returned, covering the pinned direct, split, and serial-required cases. The sanitized probe example now prints per-plan stream status, content length, content range, and first-chunk status. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download | rg 'case=|download plan:|plan file|plan stream|plan first_chunk|serial labels=|serial values='` passed.
- Inspected the legacy download and extraction implementation in `../dlsite-manager` read-only and documented the rewrite boundary in `docs/designs/2026-05-07.01-download-and-archive-boundary.md`. Added workspace crates `crates/dm-download` and `crates/dm-archive`; `dm-archive` now classifies single ZIP and legacy first-EXE split archive sets with unit tests, while `dm-download` contains initial job request, progress, cancellation, error, and stream metadata helper types. Observed v3 final stream URLs expose a `/file/{filename}/` segment and ranged responses expose total size through `Content-Range`. Validation: `cargo fmt --all`, `cargo test -p dm-download -p dm-archive`, `cargo run -p dm-api --example probe_download | rg 'case=|plan stream|plan first_chunk|download plan:'`, and `cargo test --workspace` passed, including env-enabled `dm-api` live tests.
- Added the first `dm-download` stream-to-file writer. The writer stages files under `.dm-download`, resumes from existing staging files, retries ranged streams from the current byte offset, validates filenames before joining paths, refuses finalized-target overwrites, preserves staging files on cancellation, and exposes a `DlsiteDownloadSource` adapter over `dm-api` streams. Validation: `cargo fmt --all`, `cargo test -p dm-download`, and `cargo test --workspace` passed.
- Added `dm-download` plan-level orchestration. `download_work_files` now verifies the work ID against `dm-api::DownloadPlan`, probes each planned file for final URL and `Content-Range` metadata, derives filenames/sizes, and downloads files under `{target_root}/{work_id}` through the staged writer. Validation: `cargo fmt --all`, `cargo test -p dm-download`, and `cargo test --workspace` passed.
- Added env-gated live verification for `dm-download` and documented required values in `crates/dm-download/.env.example`. The live test downloads one pinned work through `download_work_files` into a temporary directory after checking probed total size against `DMSITE_DOWNLOAD_TEST_MAX_TOTAL_BYTES`. Validation: `cargo fmt --all`, `cargo test -p dm-download`, `DMSITE_DOWNLOAD_TEST_LIVE=1 DMSITE_DOWNLOAD_TEST_MAX_TOTAL_BYTES=1000000 cargo test -p dm-download --test live -- --nocapture`, and `cargo test --workspace` passed.
- Updated `dm-api` optional serial-page probing after live validation observed HTTP 302 for a non-serial direct work where HTTP 500 had previously been observed. Optional serial probing now treats both 500 and 3xx as no serial page for already-direct downloads. Validation included the workspace test run above.
- Added deterministic ZIP extraction to `dm-archive` for `SingleZip` plans. Extraction now uses a generated staging directory, rejects unsafe archive entry paths, optionally flattens one wrapper directory, refuses output overwrites, removes source ZIPs by default, and cleans staging directories on success/error. Validation: `cargo fmt --all`, `cargo test -p dm-archive`, and `cargo test --workspace` passed.
- Wired `dm-download` archive handoff for `UnpackPolicy::UnpackWhenRecognized`. Completed downloads now call `dm-archive` for supported `SingleZip` sets, while `KeepArchives` and currently unsupported legacy split-RAR sets remain untouched. Validation: `cargo fmt --all`, `cargo test -p dm-download`, and `cargo test --workspace` passed.
- Added repo-local real-download tooling for manual live artifact inspection. `.dlsite-downloads/` is now gitignored, and `crates/dm-download/examples/download_work.rs` downloads one or more env-configured works there with a per-work size guard and optional unpacking. Observed `DMSITE_DOWNLOAD_WORK_ID=RJ296395` returned HTTP 402 Payment Required for the authenticated account; overriding to the pinned serial work `VJ01001165` downloaded `VJ01001165.zip` at 167,790 bytes under `.dlsite-downloads`. Validation: `cargo fmt --all`, `cargo check -p dm-download --example download_work`, `DMSITE_DOWNLOAD_WORK_ID=VJ01001165 DMSITE_DOWNLOAD_MAX_TOTAL_BYTES=1000000 DMSITE_DOWNLOAD_UNPACK=0 cargo run -p dm-download --example download_work`, and `git check-ignore -v .dlsite-downloads/VJ01001165/VJ01001165.zip` passed.
- Added legacy split-RAR extraction through `unrar-ng = 0.7.6`, after checking current crate availability with `cargo search`/`cargo info`. `dm-archive` now handles `LegacySplitRar` by temporarily renaming the first `.exe` part to `.rar`, validating listed entry paths, extracting through staging, restoring the first part on error, and removing source parts after success. `dm-download` now hands `LegacySplitRar` plans to `dm-archive` for `UnpackWhenRecognized`, and `crates/dm-archive/.env.example` documents an env-gated real split-RAR fixture test. Validation: `cargo fmt --all`, `cargo test -p dm-archive`, and `cargo test -p dm-download` passed.
- Updated direct Rust dependencies after checking current crate versions: `reqwest 0.13.3`, `reqwest_cookie_store 0.10.0`, `cookie_store 0.22.1`, `thiserror 2.0.18`, `tokio 1.52.2`, `zip 2.4.2`, and related direct dependencies. `reqwest` now enables explicit `form` and `query` features required by 0.13. Validation: `cargo check --workspace`, `cargo test -p dm-api`, `cargo test -p dm-download`, and `cargo test -p dm-archive` passed, including env-enabled `dm-api` and `dm-download` live tests.
- Verified the replacement real download fixture `RJ257630`. With `DMSITE_DOWNLOAD_MAX_TOTAL_BYTES=262144000` and `DMSITE_DOWNLOAD_UNPACK=1`, the example downloaded `RJ257630.zip` at 166.86 MiB into `.dlsite-downloads/RJ257630`, extracted two top-level items, and removed the ZIP source archive. `RJ296365` is not a reliable fixture because DLsite can refuse it after a download limit is reached despite account rights. Validation: `git check-ignore -v .dlsite-downloads/RJ257630` confirmed the payload directory is ignored.
- Verified the real split-RAR fixture `RJ01105050`. Ignored local `.env` files now use `RJ01105050` for the split API/download/archive fixture settings; `dm-download` downloaded `RJ01105050.part1.exe` at 3,221,225,472 bytes and `RJ01105050.part2.rar` at 256,810,543 bytes into `.dlsite-downloads/RJ01105050` with unpack disabled so the fixture remains reusable. Adjusted the archive live test to resolve relative fixture paths from the repo root. Validation: `cargo run -p dm-download --example download_work`, `git check-ignore -v .dlsite-downloads/RJ01105050/RJ01105050.part1.exe .dlsite-downloads/RJ01105050/RJ01105050.part2.rar`, and `cargo test -p dm-archive --test live_legacy_split_rar -- --nocapture` passed.
- Added the first small `dm-storage` cut. The crate uses SQLite through `sqlx 0.9.0-alpha.1` with SQLx embedded migrations, opens file-backed or in-memory databases, and exposes a `WriteTransaction` wrapper so future write APIs have an explicit transaction boundary. No domain tables were added; schema should grow only with feature work. Validation: `cargo test -p dm-storage` and `cargo test --workspace` passed.
- Added the first GUI-facing settings slice. `dm-storage` now has an `app_settings` migration plus transactional get/save APIs for library and download roots; Tauri initializes storage in the app data directory and exposes thin `get_settings`/`save_settings` commands; the Svelte start page is now a compact settings screen. Added `@types/node` and removed the stale Vite `@ts-expect-error` so frontend typecheck is warning-free. Validation: `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, and `cargo test --workspace` passed.
