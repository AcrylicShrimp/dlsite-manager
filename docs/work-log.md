# Work Log

## 2026-05-12

- Improved support diagnostics for failed jobs and audit logs. `dm-library` now exposes safe support messages/details for nested DLsite API and download errors, including API/download error kinds, HTTP status, redacted endpoints, safe body snippets, byte counts, and local paths where relevant. Tauri job failures now copy those details into job `errorDetails`, so audit entries no longer collapse download failures to only `dlsite api error`. Download state and bulk failure records now use the same support-oriented messages. Validation: `cargo fmt --all`, targeted `cargo test -p dm-library support_ -- --nocapture`, `cargo check -p dlsite-manager`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check --workspace`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.

## 2026-05-11

- Created `docs/todos/2026-05-11.01.refactor-frontend-into-reusable-components.md` to track the frontend refactor plan. The plan splits the current all-in-one Svelte page into pure model/utils extraction, shared shell/overlay components, Library components, secondary page components, and later Tauri/state cleanup, with behavior-preserving validation rules. Validation: documentation-only change; `git diff --check` passed.
- Fixed the Library control shell rounded corners by clipping only the sticky controls wrapper. This prevents square child panel backgrounds from drawing over the rounded top edge while keeping the Library page-scroll behavior intact. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added explicit ellipsis handling inside Library filter chips. Filter labels now render in a truncating span, and maker counts stay fixed so long maker/account names do not overflow their chip. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Reworked the expanded Library filter layout after the viewport-height sticky control model proved unreachable at the top of the page. The sticky region is now only the compact search/action shell; expanded filters flow below it as normal page content, so page scrolling can always reach the bottom of large maker lists. Updated the unified product-list design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Capped the sticky Library controls to the viewport height so very large expanded filter sets can scroll inside the controls block. The Library action row remains pinned at the bottom of that controls block while the product list keeps the page-level scroll model. Updated the unified product-list design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Changed Library scrolling so the workspace page scrolls instead of the product table, with the search/filter/action controls sticky at the top of the Library scroll area. Top spacing now belongs to the Library header rather than the scroll container, so the sticky controls do not leave a gap while scrolling. This keeps large expanded maker filter lists reachable while preserving separate inner scrolling on Downloads/Activity pages. Updated the unified product-list design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Removed the Library maker-facet cap. The Tauri command no longer hard-codes a 24-maker request, and storage now returns every matching maker facet while still ignoring selected maker filters for facet generation. Added a storage regression test with 30 distinct makers. Validation: `cargo fmt --all`, `cargo test -p dm-storage product_filter_facets_return_all_matching_makers`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Not committed per user request.
- Changed the Library filter panel to start collapsed by default, keeping the initial Library view focused on search and page actions until filters are explicitly expanded. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Not committed per user request.
- Moved Library action buttons into a dedicated full-width command row below the search/filter controls. Reload/Sync sit on the left, and Scan Local/Bulk Download sit on the right, so actions no longer occupy a sparse side column. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Not committed per user request.
- Normalized visible action/filter label casing across the Library, Accounts, Settings, and product action menu UI. Command buttons now use Title Case consistently, while non-command status/body text remains sentence-style. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Not committed per user request.
- Changed Library product sorting to use latest purchase as the default and reordered the selector to Latest purchase, Published, Title. The storage/Tauri default sort also now falls back to latest purchase when omitted, and the affected storage ordering test was updated for that behavior. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added per-product DLsite page actions. Library rows now include a `DLsite` button, and product detail panels include an `Open on DLsite` link; both use the existing opener plugin and the generic `home/work/=/product_id/{workId}.html` URL, which was checked against home, maniax, and soft redirects. Validation: `pnpm check`, `pnpm build`, and `git diff -- src/routes/+page.svelte` passed. Did not restart or stop the already-running dev process per user preference.
- Added About-panel external buttons for GitHub and DLsite, using the existing Tauri opener plugin `openUrl` path so links open in the system browser instead of navigating the app webview. Validation: `pnpm check`, `pnpm build`, and `git diff -- src/routes/+page.svelte` passed. Did not restart or stop the already-running dev process per user preference.
- Split Settings/About into a standalone panel below the storage-path settings form, removing the internal divider approach while keeping the Settings page full-width and scrollable. Validation: `pnpm check`, `pnpm build`, and `git diff -- src/routes/+page.svelte` passed. Did not restart or stop the already-running dev process per user preference.
- Let the Settings panel fill the workspace width by removing its fixed 760px cap and preserving min-width constraints for path fields. Validation: `pnpm check`, `pnpm build`, and `git diff -- src/routes/+page.svelte` passed. Did not restart or stop the already-running dev process per user preference.
- Raised the toast stack above all modal/dialog layers so copy/status notifications stay visible and dismissible while product detail or other dialogs are open. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Removed the separate product-detail `Copy title` button. The detail title itself is now a styled copy target with title-variant hover text, keeping the modal header cleaner while preserving keyboard focus behavior. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Tightened product detail label/value spacing by overriding global button height inside detail fields and using a consistent row gap for credit rows. This removes invisible 38px button-height padding that made Identity and Credits spacing inconsistent. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Stabilized the product detail modal layout. The detail body now uses explicit summary/metadata columns with natural-height sections instead of stretched grid rows, and detail label/value fields use stacked cells for denser, more predictable wrapping across viewport sizes. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added a first product detail view without adding duplicated persistent detail storage. `dm-storage` now assembles a read-only `ProductDetail` projection from existing cached work columns, parsed title/maker variants, parsed credits/tags from `works.raw_json`, owners, dates, content size, and durable download state; `dm-library` and Tauri expose it as `get_product_detail`; and the Library UI opens a dark detail modal from each title. The internal/debug `Cached fields` section was removed from the UI and DTO. Updated the unified product-list design note and v3 readiness tracker. Validation: `cargo fmt --all`, targeted `cargo test -p dm-storage product_detail_assembles_cached_metadata_without_new_storage`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `cargo check --workspace`, `cargo test -p dlsite-manager`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added a raw `content/works` discovery probe in `dm-api` for exact response-shape checks. `DlsiteClient` now exposes raw works-batch probing, and `probe_works` can print raw top-level keys plus serial/license-like key and literal searches with `DMSITE_API_PROBE_WORKS_RAW=1`. Live probing `VJ01001165` showed no serial-related fields or literals in the exact `POST /api/v3/content/works` JSON; serial detection remains tied to download resolution / serial-page probing. Validation: `cargo fmt --all`, `cargo check -p dm-api --examples`, and `DMSITE_API_TEST_BATCH_WORK_IDS='VJ01001165' DMSITE_API_PROBE_WORKS_RAW=1 cargo run -p dm-api --example probe_works` passed.
- Added serial-information file creation after successful downloads. `dm-library` now writes DLsite `DownloadPlan.serial_numbers` to an app-owned `dlsite-manager-serial.txt` in the staged work directory during finalization, before moving the work into the library. Serial values are not stored in SQLite, audit logs, job logs, or frontend state; the writer avoids overwriting non-app product files by using numbered fallbacks. Updated the download boundary design note and v3 release readiness tracker. Validation: `cargo fmt --all`, targeted `cargo test -p dm-library serial`, targeted `cargo test -p dm-library downloads_owned_work_and_records_local_path`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, `pnpm check`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added first-cut account removal. `dm-storage` deletes account rows transactionally and relies on SQLite cascades for ownership/sync history while preserving global work cache and download records; `dm-library` deletes the app-local saved credential before removing the account; Tauri exposes `remove_account` with audit logging; and the Accounts UI now enables Remove behind an in-app danger confirmation, disabled only during an active sync for that account. Updated the unified product-list design note and v3 release readiness tracker. Validation: `cargo fmt --all`, targeted account removal tests in `dm-storage` and `dm-library`, `cargo test -p dm-storage`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, `pnpm check`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added first-cut local-only product scan/import. `dm-library` scans direct library-root child folders, detects exactly one DLsite work ID, skips ambiguous/no-ID/existing-download folders, creates local placeholder works when needed, and records imported folders as manual downloaded state. `dm-storage` includes downloaded local-only rows in the default Library query with a synthetic `Local` owner while preserving synced metadata and excluding local-only rows from account-specific filters. Tauri exposes `scan_local_work_downloads` with audit details, and the Library toolbar has a `Scan local` action. Updated the unified product-list design note and v3 release readiness tracker. Validation: `cargo fmt --all`, targeted local import tests in `dm-storage` and `dm-library`, `cargo test -p dm-storage`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, `pnpm check`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added deterministic DLsite work-ID detection as the foundation for local-only scan/import. `dm-library` now detects `RJ`/`VJ`/`BJ` IDs in flexible folder names with 6-8 digit length checks, ASCII-alphanumeric boundaries, lowercase normalization, duplicate removal, path filename helpers, and ambiguous multi-ID handling through `detect_unique_work_id_in_path`. Updated the unified product-list design note and v3 release readiness tracker. Validation: `cargo fmt --all`, targeted `cargo test -p dm-library detects_work_ids`, targeted `cargo test -p dm-library work_id_detection`, targeted `cargo test -p dm-library detects_unique_work_id_from_path_file_name`, `cargo test -p dm-library`, `cargo check --workspace`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added manual downloaded-state marking for existing library folders. `dm-library` now records a selected existing directory under the configured library root as a downloaded work without moving files, Tauri exposes `mark_work_downloaded` with audit logging, and the Library row menu can mark a not-downloaded/failed/cancelled work as downloaded through the existing directory picker while patching the row in place. Updated the download boundary design note and v3 release readiness tracker. Validation: `cargo fmt --all`, targeted `cargo test -p dm-library mark`, targeted `cargo test -p dm-library manual_download`, `cargo test -p dm-library`, `pnpm check`, `cargo check -p dlsite-manager`, `pnpm build`, `cargo check --workspace`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added visible app version/about metadata for v3 support reports. Settings now shows runtime Tauri metadata for application name, app version, identifier, and Tauri framework version through the existing core app API, without adding a custom backend command. Updated the v3 release readiness tracker. Validation: `pnpm check`, `pnpm build`, `cargo check -p dlsite-manager`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added invalid DLsite download-response hardening for the v3 release readiness plan. `dm-download` now rejects HTML `Content-Type` during first-byte metadata probing and rejects an HTML-looking first response chunk before it can be written to resumable staging, preventing DLsite error pages from being persisted as successful archives. Updated the download boundary design note and marked the first release-readiness criterion complete. Validation: `cargo fmt --all`, targeted `cargo test -p dm-download rejects_html`, targeted `cargo test -p dm-download detects_html_download_body`, `cargo test -p dm-download` including the env-enabled live fixture, `cargo test -p dm-library`, and `cargo check --workspace` passed. Did not restart or stop the already-running dev process per user preference.
- Refreshed v3 milestone tracking after the main rewrite pieces landed. The local rewrite TODO now marks API, download/archive, storage, library orchestration, jobs, audit, and first GUI/download paths complete while leaving local-only management, manual downloaded marking, tags/hidden/detail/version, and release validation open. Updated GitHub PR #37 and the public `v3 rewrite` milestone description to remove stale OS-keychain and missing-download wording, and commented on #31/#11 as covered by the draft PR but intentionally left open until preview confirmation/release. Validation: documentation/tracking-only change; `git diff --check` passed.
- Fixed download cancellation getting stuck in `Cancelling`. `dm-library` now forwards the job cancellation token into the lower-level `dm-download` token for the full download call, and `dm-download` uses cancellation-aware waits around metadata probing plus stream open/read operations so stalled reads can return `Cancelled` promptly while preserving staging files. Added regression tests for cancellation without progress events and pending stream reads. Updated the background-job design note. Validation: `cargo fmt --all`, targeted `cargo test -p dm-download cancellation_interrupts_pending_stream_read`, targeted `cargo test -p dm-library download_cancellation_reaches_download_source_without_progress`, `cargo test -p dm-download`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, and `cargo check --workspace` passed. The full `dm-download` test run included the env-enabled live fixture. Did not restart or stop the already-running dev process per user preference.
- Fixed stale active download detail text in the queue/activity UI. Active `workDownload` and `bulkWorkDownload` jobs now derive their detail line from the current job phase/progress instead of reusing the latest log message, so `Resolving download files` no longer sticks through downloading/decompressing/finalizing states. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Fixed the Downloads queue scroll pane layout. The queue list now uses a flex column with fixed-height rows, preventing a single active download row from stretching across the remaining panel height and appearing at the bottom of the viewport. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added a dedicated Downloads navigation page between Library and Accounts. The page is a live queue view only: it filters existing job snapshots to active `workDownload` and `bulkWorkDownload` jobs, shows current/queued/running counts, progress, timestamps, and cancel controls, while finished history, failures, cancellations, and bulk planning stay in Activity/audit logs. Updated design notes to preserve that split. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Replaced the remaining destructive `window.confirm` calls with an in-app confirmation dialog for re-download and delete-download actions. The dialog uses the same dark modal style, Escape/backdrop/close cancellation, and a danger-styled confirm action; no native `window.alert`/`window.confirm` calls remain in the frontend/Tauri code searched. Validation: `pnpm check`, `pnpm build`, `rg -n "window\\.confirm|window\\.alert|confirm\\(|alert\\(" src/routes/+page.svelte src-tauri/src/lib.rs || true`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Replaced the bulk-download preview `window.alert`/`window.confirm` calls with an in-app modal dialog. The modal shows product counts, skipped downloaded/queued counts, expected total bytes, warnings for unchecked products, a no-op notice state, and Escape/backdrop/close handling so the confirmation is visible inside the Tauri webview. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Switched bulk-download preview sizing from exact download-plan probing to cached DLsite `content_size`. `dm-api` now models `content_size` and preserves unknown `content/works` fields through `Work.extra`; `dm-storage` exposes cached `content_size_bytes` on product rows; `dm-library` bulk preview sums cached sizes and marks older rows without that field as unknown instead of logging in and opening ranged download streams during planning. Added `dm-api`'s sanitized `probe_works` example and updated design notes. Validation: `cargo run -p dm-api --example probe_works`, `cargo test -p dm-api --lib`, targeted `cargo test -p dm-storage product_list_includes_content_size_from_raw_json`, targeted `cargo test -p dm-library bulk_download_preview_reports_expected_bytes`, `cargo test -p dm-storage`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, `cargo check --workspace`, `cargo check -p dm-api --examples`, `pnpm check`, `pnpm build`, and `git diff --check` passed. `cargo test -p dm-api` was also tried and the env-enabled live serial fixture still failed with DLsite returning HTTP 302 for the pinned serial page. Did not restart or stop the already-running dev process per user preference.
- Added progress tracking for bulk-download planning. `dm-library` now emits preview progress while selecting works and probing each planned work, Tauri runs `preview_bulk_work_download` internally as a visible `bulkWorkDownloadPreview` Activity job, and the Library button reflects planning progress such as `Planning N/M` while preserving the existing confirmation flow. Updated download/background-job design notes. Validation: `cargo fmt --all`, targeted `cargo test -p dm-library bulk_download_preview_reports_progress`, `cargo check -p dlsite-manager`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Renamed the Library toolbar bulk action label from `Download results` to `Bulk Download`. Validation: `pnpm check` and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Stopped download completion and delete-download flows from reloading the whole Library product list and losing visible list context. The cause was terminal `workDownload`/`bulkWorkDownload` job events and `delete_work_download` success calling `loadProducts()`, which replaced the product array from page zero. The frontend now patches affected rows in place from job/command results, while `dm-library`/Tauri bulk job output includes per-work success records with local paths so bulk completion can update rows without a full reload. Updated the unified product-list design note. Validation: `cargo fmt --all`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added process-local active download reservations so overlapping single/bulk download requests cannot claim the same work twice. Bulk preview and queueing now treat already-downloaded and already-queued/downloading matches as no-op skips, report both skip counts in the confirmation/audit/job metadata, and pass only currently reserved work IDs into `dm-library`; duplicate single-work requests create a short no-op job instead of starting another transfer. The Library bulk button no longer globally blocks while another bulk job is active, because overlap is handled per work ID. `dm-library` bulk download/preview can now be constrained to explicit work IDs. Updated download/background-job design notes. Validation: targeted `cargo test -p dm-library bulk_download_limits_to_supplied_work_ids`, `cargo test -p dlsite-manager download_reservations`, `cargo check -p dlsite-manager`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added a bulk-download preview before queueing downloads. `dm-library` can now select the same filtered bulk work set, resolve each DLsite download plan, probe first-byte stream metadata for expected sizes, and return selected/checked/skipped counts plus expected bytes without writing files. Tauri exposes `preview_bulk_work_download`, audits preview success/failure, and the Library button now shows a planning state plus a confirmation alert with product count and expected total bytes before starting `bulkWorkDownload`. Updated download/background-job design notes. Validation: `cargo fmt --all`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added first-cut bulk download for the Library results. `dm-library` now owns sequential cache-query bulk download orchestration over the existing single-work downloader, skips already-downloaded works by default, records per-work failures while continuing later works, and reports structured summaries. Tauri exposes a thin `bulkWorkDownload` job command with audit details, and the Library toolbar adds a confirmed `Download results` action for the current filters/search/sort. Updated download/background-job design notes. Validation: `cargo fmt --all`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check -p dlsite-manager`, `pnpm check`, and `pnpm build` passed. Did not restart or stop the already-running dev process per user preference.
- Hid per-file metadata probing from visible work-download status. Tauri now ignores `dm-download::DownloadPhase::ProbingMetadata` for work-download job phases so split downloads no longer flash `Checking file` between aggregate `Downloading N%` updates; the frontend also defensively avoids the old `Checking file` label for any stale `probingDownload` snapshot. Updated the background-job design note. Validation: `cargo fmt --all`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `cargo test -p dm-download`, `pnpm check`, `pnpm build`, and `git diff --check` passed. The env-enabled `dm-download` live test also ran and passed. Did not restart or stop the already-running dev process per user preference.
- Fixed work-download progress for multi-file/split products and decompression visibility. `dm-download` now probes all planned files before streaming, emits aggregate work-level byte progress across all files, and emits `Unpacking` before recognized archive extraction; the frontend renders that phase as `Decompressing` in both Activity and row actions. Updated download/background-job design notes. Validation: `cargo fmt --all`, `cargo test -p dm-download`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, and `git diff --check` passed. The env-enabled `dm-download` live test also ran and passed. Did not restart or stop the already-running dev process per user preference.
- Fixed Activity panel scrolling. The Activity view now uses a bounded grid, Jobs and Audit panels are flex columns, and the Jobs/Audit lists scroll independently inside their panels instead of stretching the whole screen. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Removed the account editor enabled checkbox. New accounts now default to enabled, while edits preserve an existing account's enabled/disabled state; source availability remains controlled by the row-level enabled/disabled action. Cleaned up the now-unused checkbox CSS. Validation: `cargo fmt --all`, `cargo test -p dlsite-manager`, `cargo check -p dlsite-manager`, `pnpm check`, and `pnpm build` passed. Did not restart or stop the already-running dev process per user preference.
- Removed the GUI remember-password option. Account save commands now always save/update credentials through the app-local vault, the Accounts form only exposes account enabled/disabled as a checkbox, and the credential design note records that remembered credentials are mandatory in the GUI. Validation: `cargo fmt --all`, `cargo test -p dlsite-manager`, `cargo check -p dlsite-manager`, `pnpm check`, and `pnpm build` passed. Did not restart or stop the already-running dev process per user preference.
- Replaced the native keychain credential path with an OS-free app-local vault. `dm-credentials` now stores remembered passwords in a caller-selected JSON vault file with private Unix file/directory permissions and deterministic local tests; platform keychain dependencies and the env-gated native keychain test were removed. Tauri initializes the vault at `app_data_dir/credentials/vault.json`, and account DTOs report `Credential: Saved` only when the current vault can actually load the referenced password. Updated credential design notes and the rewrite milestone TODO to record the app-local vault policy. Existing accounts from the keychain-backed experiment may need their credential updated once. Validation: `cargo fmt --all`, `cargo test -p dm-credentials`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added a file-backed audit log first cut. New `dm-audit` writes sanitized append-only `audit-YYYY-MM-DD.jsonl` files under Tauri's app log directory, while Tauri initializes JSON runtime tracing through `tracing`, `tracing-subscriber`, and `tracing-appender`. The Tauri layer records key operation queue/success/failure/cancel outcomes, exposes audit event/list/open-log-dir commands, and Activity now shows recent audit events plus an Open folder action. Added the audit log design note. Validation: `cargo fmt --all`, `cargo test -p dm-audit`, `cargo check -p dlsite-manager`, `cargo test -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, `pnpm tauri build --debug`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added single-work download lifecycle row actions. `dm-storage` can clear a work download record transactionally; `dm-library` can safely delete downloaded/staging paths under configured roots and supports staged re-download replacement; Tauri exposes `delete_work_download` and `start_work_download.replace_existing`; the Library row menu now offers archive-only download, confirmed re-download, and confirmed delete download. Updated the download boundary design note. Validation: `cargo fmt --all`, `cargo test -p dm-storage product_list_includes_download_state`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri build --debug`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Replaced frontend direct opener use for downloaded works with a guarded Tauri command. `open_work_download` now accepts a work ID, reads the persisted download path from storage, validates the resolved path under configured library/staging roots, and opens it from Rust via the native opener; the frontend no longer needs `opener:allow-open-path`. Updated the download boundary design note. Validation: `cargo fmt --all`, `cargo test -p dlsite-manager path_root_check`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri build --debug`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Granted the Tauri opener capability needed by the Library row `Open` action. `src-tauri/capabilities/default.json` now includes `opener:allow-open-path` so downloaded local work folders can be opened through `@tauri-apps/plugin-opener`. Validation: `cargo check -p dlsite-manager`, `pnpm tauri build --debug`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Stabilized the Library list during active downloads. Tauri now coalesces high-volume work-download byte progress into roughly one frontend job event per second while keeping phase/terminal updates immediate; the Library scroll container now uses a content-independent flex basis, stable scrollbar gutter, and disabled scroll anchoring on rows. Updated the background-job design note with the one-second download progress cadence. Validation: `cargo fmt --all`, `cargo test -p dlsite-manager work_download_progress_throttle`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added tooltip help and known DLsite work-type labels for Library classification chips. The frontend now expands codes such as `SLN` to readable labels like `Simulation`, assigns the correct product-family tone, and gives age chips hover help. `dm-storage` also recognizes additional exact DLsite work-type codes when grouping cached products, with `SLN` covered by the game filter. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `cargo test -p dm-library`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Replaced native browser help for Library classification chips with an app-styled tooltip and rewrote the copy to lead with human product vocabulary, e.g. `Utility: Utility tool or app. DLsite code TOL.` Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Reclassified DLsite voice comic (`VCM`) as part of the image/comic product filter while giving it a dedicated sky-blue card/chip tone in the Library UI. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added the first single-work GUI download path. `dm-storage` now persists per-work download state, `dm-library` selects an enabled owning account and moves successful downloads from staging to the library folder, Tauri exposes a `workDownload` job, and Library row actions now start downloads, show active progress, retry failed/cancelled rows, and open downloaded paths. `dm-download` now reuses already-finalized files when their expected size matches, improving retry/resume behavior. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo test -p dm-library`, `cargo test -p dm-download --lib`, `cargo check -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, and `git diff --check` passed. `cargo test -p dm-download` was also run and its env-enabled live test failed with DLsite returning HTTP 302 for the pinned serial page; the crate unit tests passed. Did not restart or stop the already-running dev process per user preference.
- Re-ran `cargo test -p dm-download` after the live download fixture env was updated to a currently downloadable non-serial work. The full crate test suite, including the env-enabled live download test, passed. No code changes.

## 2026-05-10

- Created `docs/todos/2026-05-10.00.add-generic-background-job-system.md` to track the proposed generic background job system. The plan covers a `dm-jobs` crate, cooperative cancellation, snapshots, bounded logs, generic Tauri job commands/events, account-sync migration, frontend job state, observability rules, and later download/archive integration. Validation: documentation-only change; inspected the generated TODO content, no code validation run.
- Added `docs/designs/2026-05-10.00-background-job-system.md` and the first `crates/dm-jobs` implementation. `dm-jobs` now owns process-local job lifecycle state, snapshots, generic events, bounded logs, cooperative cancellation, and finished-job cleanup without depending on DLsite/storage/Tauri code. `dm-library` now reuses the shared cancellation token through a public re-export. Validation: `cargo fmt --all`, `cargo test -p dm-jobs -p dm-library`, `cargo test --workspace`, `cargo check --workspace`, and `git diff --check` passed. The workspace test run included env-enabled live tests.
- Wired account sync through the generic background job system. `src-tauri` now owns a `JobManager`, forwards `dm-job-event`, exposes job start/list/get/cancel/log/clear commands, and starts account sync jobs that call `dm-library` with job progress and cancellation. The Products UI now queues sync jobs, renders active/recent job activity, supports cancellation, and refreshes account/product cache views when sync jobs finish. Validation: `cargo fmt --all`, `cargo test --workspace`, `cargo check --workspace`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed. The workspace test run included env-enabled live tests.
- Fixed account sync handling for DLsite `content/works` subset responses. `dm-library` now treats missing work-detail rows as placeholder cached works keyed by the purchased work ID instead of failing the whole sync with `MissingWorkDetails`; sync reports include `missing_detail_count`, and Tauri job output/logs surface the count. Updated the product-list design note to record the placeholder behavior. Validation: `cargo fmt --all`, `cargo test -p dm-library -p dm-storage -p dm-jobs`, `cargo check --workspace`, `pnpm check`, and `git diff --check` passed.
- Added age-category filtering to the cached product list. `dm-storage` now supports optional filters for DLsite `all`, `r15`, and `r18` age categories, the Tauri list-products DTO forwards the filter, and the Products toolbar has an explicit `Any age` unfiltered state plus `All ages`, `R-15`, and `R-18` category filters. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.
- Split the first GUI into dedicated Products, Accounts, Activity, and Settings views while keeping the existing CSS styling approach. Products now uses the full workspace width for the cached library list, Accounts owns source/account management, and Activity owns background job status/cancellation. Updated the unified product-list design note to reflect the dedicated screens. Validation: `pnpm check`, `pnpm build`, `cargo check -p dlsite-manager`, `pnpm tauri dev`, and `git diff --check` passed. The first Tauri smoke attempt found a stale repo-local dev process holding port 1420; stopping it and rerunning succeeded.
- Converted the current CSS-only GUI to a dark default theme. The Svelte page now uses neutral dark surfaces, muted text, green-accented primary actions/focus states, dark form controls, and dark product/account/job rows. The design note records that Tailwind migration remains a later separate slice. Validation: `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.
- Moved Activity out of the main sidebar navigation group and pinned it to the bottom utility area of the menu. The product-list view is now labeled Library, and the Reload/Sync actions remain scoped to that Library page. The design note now records Library, Accounts, and Settings as the main group with Activity separated for job/status monitoring. Validation: `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.
- Removed the global workspace-header action button area so page controls are no longer detached from their content. Library now keeps Search, Reload, and Sync in the library toolbar; Accounts keeps New/Reload/Sync controls in account panels; Activity keeps Reload/Clear in its job panel; Settings keeps Reload in the settings panel. Updated the product-list design note to record local page controls. Validation: `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed. The Tauri smoke first found a stale repo-local dev process on port 1420; stopping it and rerunning succeeded.
- Captured the agreed Library product-card redesign in `docs/designs/2026-05-10.01-library-product-card.md`. The design keeps maker and source-credit rows together in one metadata block, groups v3 `raw_json.tags` credits by role (`CV`, `Illust`, `Scenario`, `Creator`, `Music`, `Other`), reserves chips for classification/status, and defers functional download/serial actions until the backend exposes those states. Validation: documentation-only design capture; no code validation run.
- Extended the Library product-card design with color semantics. The left belt and type chip are product-type only; audio/voice is yellow, video is pink, games are purple/violet, CG/manga/PDF/reading material is green, and unknown/other is gray. Age chips are separate dimmed green/yellow/red for all-ages/R-15/R-18. Validation: documentation-only design update; no code validation run.
- Implemented the first Library product-card redesign. `dm-storage` now derives grouped source-credit metadata from cached v3 `raw_json.tags`, Tauri forwards the groups to the frontend, and the Library list renders square thumbnails, product-type belts/chips, age chips, copyable work IDs, maker/credit rows with native hover tooltips, owner tags, and disabled placeholder row actions for the later download-status slice. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, `cargo test --workspace`, `git diff --check`, and `pnpm tauri dev` smoke passed; the smoke was stopped after the app launched.
- Tightened the Library product-card metadata grid spacing. The maker/credit rows now use responsive CSS variables for metadata column gaps, label width, and label/value gaps so the grid compacts at narrower widths without changing the data contract. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed.
- Constrained the Library product-card metadata block width after wide-screen review showed the second credit column drifting too far right. The maker/source-credit grid now stays left-aligned with a responsive max width on large screens and falls back to full width on narrower layouts. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed.
- Replaced the inline page status line with a frontend toast stack. Success/info/error feedback now appears as fixed bottom-right popup notifications with auto-dismiss, manual dismiss, and alert semantics for errors; sync-all sequencing no longer depends on a rendered error string. Validation: `pnpm check`, `pnpm build`, `git diff --check`, and `pnpm tauri dev` smoke passed. The first smoke attempt found a stale repo-local dev process on port 1420; stopping it and rerunning succeeded.
- Replaced the toast close button text with an icon-only SVG close control while preserving the accessible dismiss label. Validation: `pnpm check` and `pnpm build` passed.
- Added explicit labels for Library product-card tag and ownership rows. Type/age chips now live under a `Tags` field, account-owner chips live under an `Owned by` field, and these labeled chip rows are vertically centered with a slightly wider shared label column for responsive layouts. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed.
- Constrained Library scrolling to the product list area and stabilized product-row layout. The app shell now uses a fixed viewport grid, the Library panel is a flex column, toolbar/count header stay fixed in the panel, the product table owns vertical scrolling, and product cards use equal row heights with clipped metadata so rows do not overlap when labeled fields grow. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Made product-card credit rendering deterministic. `dm-storage` now sorts credit tags, folds unknown `*_by` source-credit classes into `Other`, and the Library UI always renders the full known credit field set (`Maker`, `CV`, `Illust`, `Scenario`, `Creator`, `Music`, `Other`) with muted `-` placeholders for missing values. Updated the product-card design note to record the fixed field contract. Validation: `cargo test -p dm-storage`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Made Library product-card credit fields copyable. Non-missing credit rows are accessible buttons that copy the displayed value to the clipboard and emit the existing toast notification; missing `-` rows stay disabled. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Redesigned the Accounts screen as source management. The account list is now primary, with summary counts, selected-row styling, explicit enabled/disabled pills, sync/credential/last-sync metadata, and a secondary add/edit detail panel. Updated the unified product-list/account design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Reshaped account source rows into taller cards matching the account-management sketch. The enabled/disabled state is now a top-left toggle badge, identity sits as the left block, sync metadata renders as a right-side label/value stack, and row actions are limited to sync/cancel until account removal is designed. Updated the account design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Tightened account source rows after visual review showed the taller card layout felt sparse and disconnected. Account rows now use a compact shared grid with smaller type, a row-level enabled toggle, identity, three metadata cells, and a right-aligned sync action. Updated the account design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Realigned account source rows to the clarified target layout: enabled/disabled toggle on its own top line, identity on the left, metadata as a right-side label/value stack, and actions below the metadata. Added row actions for sync/cancel, update credential via the existing account editor, and a disabled Remove placeholder until account deletion is implemented. Updated the account design note. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Improved Settings path management. The settings UI now explains library vs download staging semantics, defaults the download staging folder to the system Downloads directory when unset, and uses Tauri's dialog plugin for directory selection. Updated the storage design note and Tauri dialog capability. Validation: `cargo fmt --all`, `pnpm check`, `cargo check -p dlsite-manager`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Hid missing-detail placeholder works from the Library product list while preserving their cache and ownership rows. `dm-storage` now filters `raw_json.detail_status = "missing_from_content_works"` from product list queries, and `dm-library` tests keep the sync report/cache behavior while expecting only detailed products in the visible list. Updated the unified product-list design note. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Switched cached Library product images from DLsite `work_files.sam` to `work_files.main`. `dm-api` already parsed both values; `dm-library` now stores the main image URL in `thumbnail_url`, with a regression assertion in the account sync test. Validation: `cargo fmt --all`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added a Library product image preview. Product thumbnails with image URLs are now clickable buttons that open a large modal preview using the cached main image, with title/work ID context, backdrop close, close icon, and Escape handling. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Hardened the Library product query contract for later bulk actions. `dm-storage` now supports a storage-backed product type group filter (`audio`, `video`, `game`, `image`, `other`) and searches cached DLsite tag/credit names from `works.raw_json.tags` in addition to work ID, title, and maker. Tauri forwards the new filter, and the Library toolbar exposes an `Any type` selector. Updated the unified product-list design note. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, `cargo test -p dm-library`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Started the frontend refactor with a behavior-preserving model extraction. Moved frontend DTO/view types from `src/routes/+page.svelte` to `src/lib/model/types.ts` and static URLs/filter/product-type metadata to `src/lib/model/constants.ts`; the page now imports those modules while retaining state, markup, and helpers for the next slices. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Continued frontend Phase 1 by moving generic formatting helpers into `src/lib/utils/format.ts`: byte labels, bulk expected-byte labels, text variant labels, detail placeholders/dates, download status labels, nullable strings, error messages, and app-info display values. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Continued frontend Phase 1 by moving product-domain helpers into `src/lib/utils/products.ts`: DLsite product-type mapping/fallbacks, age labels/tooltips, credit-field rows, credit tooltip text, and detail tag filtering. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Continued frontend Phase 1 by moving pure account display helpers into `src/lib/utils/accounts.ts`: login, credential, enabled, last-sync labels, and enabled/credentialed counts. Job-aware account status/count helpers remain local for the next job utility slice. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Continued frontend Phase 1 by moving pure job/audit/download-queue helpers into `src/lib/utils/jobs.ts`: job upserts, status/phase labels, progress text, output/metadata parsing, bulk-download result parsing, active detail text, audit labels, and queue progress helpers. Stateful selectors that depend on page-owned arrays remain local for the later component/controller split. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Started frontend Phase 2 by extracting `src/lib/components/ToastStack.svelte`. Toast state and timers remain in `+page.svelte`, while notification markup and scoped styles now live in a reusable component. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Continued frontend Phase 2 by extracting `src/lib/components/ConfirmationDialog.svelte`. Generic confirmation state remains in `+page.svelte`, while overlay markup/buttons/styles are now reusable and scoped. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added a small frontend UI primitive layer under `src/lib/components/ui`: `Button.svelte`, `TextInput.svelte`, and `Field.svelte`. Applied these primitives to the Settings page controls only, keeping page-specific buttons local until their owning components are extracted. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Recorded owner confirmation that implemented v3 functionality is good. Updated GitHub PR #37 and the public `v3 rewrite` milestone description to reflect confirmed implemented features; closed confirmed issues #4, #11, #30, #31, #32, and #33 with implementation notes. Left #35 open for the PR merge/release path and left #18/#19/#20/#21/#22 open for tags/hidden/search/category follow-up or explicit deferral. Validation: tracking-only change; `git diff --check` passed.

## 2026-05-09

- Captured the unified multi-account product-list design in `docs/designs/2026-05-09.00-unified-product-list-and-accounts.md`. The design treats DLsite accounts as ownership sources under one virtual library, keeps list rendering cache-backed, defers serial/session-sensitive storage, and sets the next implementation slice for account/work ownership storage plus a `dm-library` orchestration crate. Validation: documentation-only change; no code validation run.
- Added `crates/dm-credentials` for remembered account passwords outside SQLite. The crate defines credential references, a `CredentialStore` trait, a native OS credential-store implementation through `keyring-core` plus target-native store crates, an in-memory test implementation, and an optional env-gated native keychain test. Updated the unified product-list design and milestone TODO so account rows store only non-secret `credential_ref` values. Validation: `cargo fmt --all`, `cargo test -p dm-credentials`, `cargo tree -p dm-credentials --depth 2`, and `cargo check --workspace` passed.
- Added the unified product-list storage cache in `dm-storage`: migrations for `accounts`, `works`, `account_works`, and `sync_runs`; transactional repository APIs for account metadata, sync commits/failures, and cached list queries; and tests for account CRUD, rollback, duplicate ownership collapse, disabled-account filtering, stale ownership, failed sync records, search, and sort. Updated storage/product-list design notes and the milestone TODO. Validation: `cargo fmt --all`, `cargo test -p dm-storage`, and `cargo check --workspace` passed.
- Added `crates/dm-library` as the first account/product-list orchestration layer. It wraps `dm-storage` and `dm-credentials` for account save/list/password-reference behavior, exposes cached product-list queries, defines cancellable/progress-emitting sync requests, maps `dm-api` purchases/works into storage cache rows, records sync success/failure/cancellation, and provides a `DlsiteSyncSource` adapter. `dm-storage` also gained explicit cancelled sync-run recording. Validation: `cargo fmt --all`, `cargo test -p dm-storage -p dm-library`, `cargo tree -p dm-library --depth 2`, `cargo check --workspace`, and `git diff --check` passed.
- Added the first account/product Tauri integration. `src-tauri` now initializes `dm-library` with native `dm-credentials` when available, falls back to in-memory credentials if native storage is unavailable, exposes thin settings/account/product/sync commands, and emits sync progress events. The Svelte page now defaults to Products, shows cached unified products, includes account source management, and keeps settings as a secondary view. Validation: `cargo check -p dlsite-manager`, `cargo test -p dm-storage -p dm-library`, `cargo check --workspace`, `pnpm check`, `pnpm build`, `pnpm tauri dev`, and `git diff --check` passed.

## 2026-05-07

- Created `docs/todos/2026-05-07.00.set-up-rewrite-milestones.md` to track the staged rewrite plan: DLsite API v3 crate, download/decompression crate boundary, SQLite storage crate, and final Tauri GUI integration. Validation: inspected generated TODO content; no code validation run because this was documentation-only planning.
- Captured the agreed first-slice API crate design in `docs/designs/2026-05-07.00-dlsite-api-v3-surface.md`. The design keeps `dm-api` v3-first, owns auth/session and authenticated byte streams, delays split/download manifest contracts until live behavior is observed, and requires env-gated live tests. Validation: documentation-only change; no code validation run.
- Added the initial `crates/dm-api` Rust crate and root Cargo workspace with resolver `3`. The crate now covers v3 login/session import/export, content count, sales, works lookup with auto chunking, conservative download redirect resolution, authenticated direct download streams, a raw download probe, env-gated live tests, and `crates/dm-api/.env.example`. Live DTO handling was adjusted for observed nullable `regist_date`, `genre_ids`, and `tags`. Added `/target` to `.gitignore`. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo test --workspace` passed.
- Updated `docs/todos/2026-05-07.00.set-up-rewrite-milestones.md` to `Partially Taken`, marking the API env/test harness criterion complete while leaving full download API discovery, download/decompression, storage, and GUI milestones open. Validation: documentation-only TODO update after the workspace tests had passed.
- Updated `dm-api` to model `content/count` metadata: `production`, `page_limit`, and `concurrency`. The client now caches `page_limit` as the `content/works` batch limit during session validation and explicit count calls, while retaining header/body batch-limit fallback detection. Validation: `cargo fmt --all` and `cargo test -p dm-api` passed, including env-enabled live tests.
- Strengthened `dm-api` download and live-test coverage using pinned direct, split, and serial-required work IDs. Added `DownloadProbe`, explicit byte ranges for safe one-byte stream checks, manual redirect following for direct download streams, raw GET probing for redirect pages, and diagnostics for rejected live `content/works` chunks. Observed that split redirects can use `/home/download/split`, direct stream URLs can redirect again before the archive response, and `content/works` may return a subset of requested IDs. Fixed one malformed control sequence in the local ignored `.env` batch fixture. Validation: `cargo fmt --all` and `cargo test -p dm-api` passed with env-enabled live tests and the 1223-ID batch fixture.
- Probed split and serial-required follow-up pages and added typed parsing helpers in `dm-api`. Split pages now parse numbered part stream requests from `/home/download/=/number/{N}/product_id/{WORK_ID}.html`; serial-required pages now parse the normal direct download link from `/home/download/=/product_id/{WORK_ID}.html`. Added `crates/dm-api/examples/probe_download.rs` as a sanitized live probe tool. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download` passed.
- Added serial-number extraction for serial-required pages. `dm-api` now parses the observed `シリアル番号` table into `SerialNumber` label/value pairs and redacts serial values in `Debug`; the sanitized probe example reports counts and labels without printing serial values. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download | rg 'case=serial|serial numbers=|serial redacted values=|serial download url='` passed.
- Added unified `DownloadPlan` generation to `dm-api`. The plan normalizes direct, split, and serial-required downloads into `DownloadFile` entries plus optional serial-number metadata; serial-required works use the same `Direct` file kind as normal direct works. Optional serial-page probing treats an observed HTTP 500 for non-serial works as no serial page. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download | rg 'case=|download plan:|plan file|serial labels=|serial values='` passed.
- Strengthened download proof around the unified `DownloadPlan` surface. Live tests now open every planned file as an authenticated first-byte ranged stream and assert that bytes are returned, covering the pinned direct, split, and serial-required cases. The sanitized probe example now prints per-plan stream status, content length, content range, and first-chunk status. Validation: `cargo fmt --all`, `cargo test -p dm-api`, and `cargo run -p dm-api --example probe_download | rg 'case=|download plan:|plan file|plan stream|plan first_chunk|serial labels=|serial values='` passed.
- Inspected the legacy download and extraction implementation in `../dlsite-manager` read-only and documented the rewrite boundary in `docs/designs/2026-05-07.01-download-and-archive-boundary.md`. Added workspace crates `crates/dm-download` and `crates/dm-archive`; `dm-archive` now classifies single ZIP and legacy first-EXE split archive sets with unit tests, while `dm-download` contains initial job request, progress, cancellation, error, and stream metadata helper types. Observed v3 final stream URLs expose a `/file/{filename}/` segment and ranged responses expose total size through `Content-Range`. Validation: `cargo fmt --all`, `cargo test -p dm-download -p dm-archive`, `cargo run -p dm-api --example probe_download | rg 'case=|plan stream|plan first_chunk|download plan:'`, and `cargo test --workspace` passed, including env-enabled `dm-api` live tests.
- Added the first `dm-download` stream-to-file writer. The writer stages files under `.dm-download`, resumes from existing staging files, retries ranged streams from the current byte offset, validates filenames before joining paths, refuses finalized-target overwrites, preserves staging files on cancellation, and exposes a `DlsiteDownloadSource` adapter over `dm-api` streams. Validation: `cargo fmt --all`, `cargo test -p dm-download`, and `cargo test --workspace` passed.
- Added `dm-download` plan-level orchestration. `download_work_files` now verifies the work ID against `dm-api::DownloadPlan`, probes each planned file for final URL and `Content-Range` metadata, derives filenames/sizes, and downloads files under `{target_root}/{work_id}` through the staged writer. Validation: `cargo fmt --all`, `cargo test -p dm-download`, and `cargo test --workspace` passed.
- Added env-gated live verification for `dm-download` and documented required values in `crates/dm-download/.env.example`. The live test downloads one pinned work through `download_work_files` into a temporary directory after checking probed total size against `DMSITE_DOWNLOAD_TEST_MAX_TOTAL_BYTES`. Validation: `cargo fmt --all`, `cargo test -p dm-download`, `DMSITE_DOWNLOAD_TEST_LIVE=1 DMSITE_DOWNLOAD_TEST_MAX_TOTAL_BYTES=1000000 cargo test -p dm-download --test live -- --nocapture`, and `cargo test --workspace` passed.
- Updated `dm-api` optional serial-page probing after live validation observed HTTP 302 for a non-serial direct work where HTTP 500 had previously been observed. Optional serial probing now treats both 500 and 3xx as no serial page for already-direct downloads. Validation included the workspace test run above.
- Added deterministic ZIP extraction to `dm-archive` for `SingleZip` plans. Extraction now uses a generated staging directory, rejects unsafe archive entry paths, optionally flattens one wrapper directory, refuses output overwrites, removes source ZIPs by default, and cleans staging directories on success/error. Validation: `cargo fmt --all`, `cargo test -p dm-archive`, and `cargo test --workspace` passed.
- Wired `dm-download` archive handoff for `UnpackPolicy::UnpackWhenRecognized`. Completed downloads now call `dm-archive` for supported `SingleZip` sets, while `KeepArchives` and currently unsupported legacy split-RAR sets remain untouched. Validation: `cargo fmt --all`, `cargo test -p dm-download`, and `cargo test --workspace` passed.
- Added repo-local real-download tooling for manual live artifact inspection. `.dlsite-downloads/` is now gitignored, and `crates/dm-download/examples/download_work.rs` downloads one or more env-configured works there with a per-work size guard and optional unpacking. Observed `DMSITE_DOWNLOAD_WORK_ID=RJ296395` returned HTTP 402 Payment Required for the authenticated account; overriding to the pinned serial work `VJ01001165` downloaded `VJ01001165.zip` at 167,790 bytes under `.dlsite-downloads`. Validation: `cargo fmt --all`, `cargo check -p dm-download --example download_work`, `DMSITE_DOWNLOAD_WORK_ID=VJ01001165 DMSITE_DOWNLOAD_MAX_TOTAL_BYTES=1000000 DMSITE_DOWNLOAD_UNPACK=0 cargo run -p dm-download --example download_work`, and `git check-ignore -v .dlsite-downloads/VJ01001165/VJ01001165.zip` passed.
- Added legacy split-RAR extraction through `unrar-ng = 0.7.6`, after checking current crate availability with `cargo search`/`cargo info`. `dm-archive` now handles `LegacySplitRar` by temporarily renaming the first `.exe` part to `.rar`, validating listed entry paths, extracting through staging, restoring the first part on error, and removing source parts after success. `dm-download` now hands `LegacySplitRar` plans to `dm-archive` for `UnpackWhenRecognized`, and `crates/dm-archive/.env.example` documents an env-gated real split-RAR fixture test. Validation: `cargo fmt --all`, `cargo test -p dm-archive`, and `cargo test -p dm-download` passed.
- Updated direct Rust dependencies after checking current crate versions: `reqwest 0.13.3`, `reqwest_cookie_store 0.10.0`, `cookie_store 0.22.1`, `thiserror 2.0.18`, `tokio 1.52.2`, `zip 2.4.2`, and related direct dependencies. `reqwest` now enables explicit `form` and `query` features required by 0.13. Validation: `cargo check --workspace`, `cargo test -p dm-api`, `cargo test -p dm-download`, and `cargo test -p dm-archive` passed, including env-enabled `dm-api` and `dm-download` live tests.
- Verified the replacement real download fixture `RJ257630`. With `DMSITE_DOWNLOAD_MAX_TOTAL_BYTES=262144000` and `DMSITE_DOWNLOAD_UNPACK=1`, the example downloaded `RJ257630.zip` at 166.86 MiB into `.dlsite-downloads/RJ257630`, extracted two top-level items, and removed the ZIP source archive. `RJ296365` is not a reliable fixture because DLsite can refuse it after a download limit is reached despite account rights. Validation: `git check-ignore -v .dlsite-downloads/RJ257630` confirmed the payload directory is ignored.
- Verified the real split-RAR fixture `RJ01105050`. Ignored local `.env` files now use `RJ01105050` for the split API/download/archive fixture settings; `dm-download` downloaded `RJ01105050.part1.exe` at 3,221,225,472 bytes and `RJ01105050.part2.rar` at 256,810,543 bytes into `.dlsite-downloads/RJ01105050` with unpack disabled so the fixture remains reusable. Adjusted the archive live test to resolve relative fixture paths from the repo root. Validation: `cargo run -p dm-download --example download_work`, `git check-ignore -v .dlsite-downloads/RJ01105050/RJ01105050.part1.exe .dlsite-downloads/RJ01105050/RJ01105050.part2.rar`, and `cargo test -p dm-archive --test live_legacy_split_rar -- --nocapture` passed.
- Added the first small `dm-storage` cut. The crate uses SQLite through `sqlx 0.9.0-alpha.1` with SQLx embedded migrations, opens file-backed or in-memory databases, and exposes a `WriteTransaction` wrapper so future write APIs have an explicit transaction boundary. No domain tables were added; schema should grow only with feature work. Validation: `cargo test -p dm-storage` and `cargo test --workspace` passed.
- Added the first GUI-facing settings slice. `dm-storage` now has an `app_settings` migration plus transactional get/save APIs for library and download roots; Tauri initializes storage in the app data directory and exposes thin `get_settings`/`save_settings` commands; the Svelte start page is now a compact settings screen. Added `@types/node` and removed the stale Vite `@ts-expect-error` so frontend typecheck is warning-free. Validation: `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, `pnpm check`, `pnpm build`, and `cargo test --workspace` passed.

## 2026-05-12

- Clarified remaining v3 GitHub tracking after owner confirmation. Closed #22 because click-to-copy plus paste into Library search is accepted as sufficient; kept #19 open because its body tracks searchable/excludable local-only or `Not Owned` metadata, not generic cached tag search. Updated PR #37, the public `v3 rewrite` milestone, and local v3 TODO trackers to remove stale click-to-search wording. Validation: tracking/documentation-only change; `git diff --check` passed.
- Recorded owner confirmation that the first v3 preview packaging/release validation path has been exercised and works well. Updated local v3 TODO trackers, PR #37, and the public `v3 rewrite` milestone so packaging validation is no longer listed as a preview blocker. Validation: tracking/documentation-only change; `git diff --check` passed.
- Prepared the first v3 release-candidate metadata as `3.0.0-rc.1`. Updated `package.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, internal crate manifests, and `Cargo.lock`; also replaced scaffold package metadata with the project description and author. Marked the v3 readiness and rewrite milestone TODOs complete with #18/#19/#20/#21 deferred beyond RC1, removed those follow-up issues from the public `v3 rewrite` milestone, and updated PR #37/milestone text. Validation: `cargo check --workspace`, `pnpm check`, `pnpm tauri build`, and `git diff --check` passed. The build produced `target/release/bundle/macos/dlsite-manager.app` and `target/release/bundle/dmg/dlsite-manager_3.0.0-rc.1_aarch64.dmg`.
- Published the GitHub prerelease `v3.0.0-rc.1` from the pushed `rework/v3` branch and attached the macOS DMG artifact. The release is marked as a prerelease and documents confirmed validation plus #18/#19/#20/#21 deferrals. Validation: `gh release create` completed and returned the release URL.
- Implemented app-owned custom tags for products. `dm-storage` now has a `work_custom_tags` migration, transactional replacement writes, custom tag facets, search/include/exclude query filters, and product/detail projections; `dm-library` exposes a thin update method; Tauri exposes `set_product_custom_tags` plus updated list/bulk query DTOs; and the Library/detail UI can add/remove/copy custom tags, display them on rows, and include/exclude them from filters. Updated the unified product-list design note and local TODO trackers. Validation so far: `cargo fmt --all`, targeted `cargo test -p dm-storage product_custom_tags_are_searchable_filterable_and_excludable`, `cargo test -p dm-storage`, `cargo check -p dlsite-manager`, and `pnpm check` passed. Did not restart or stop the already-running dev process per user preference.
- Reused the existing product chip styling for custom tags in the product detail panel and reduced the remove icon sizing/padding so custom tags align with other chips. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Applied semantic color theming to Library age/type filter chips. Age filters now use muted all-ages/R-15/R-18 tones, and work-type filters reuse the existing audio/video/game/image/other palette from product rows while keeping the neutral `Any` controls unchanged. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Dimmed inactive Library age/type filter chips so unselected semantic options stay subdued while active and hover states remain readable. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Removed the read-only DLsite metadata `Tags` section from the product detail modal and rearranged the modal stacks so Identity/Credits/Dates are on the left while Ownership/Download/Custom Tags are on the right. Removed the now-unused frontend detail-tag helper. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Added work ID context to product-related toast messages. Copy actions, custom-tag edits, single-product download queue/delete/mark actions, and cancellation requests now include the related work ID when available. Validation: `pnpm check`, `pnpm build`, and `git diff --check` passed. Did not restart or stop the already-running dev process per user preference.
- Updated public v3 tracking for confirmed custom-tag functionality. PR #37 and the `v3 rewrite` milestone now list custom product tags as implemented, #18 was assigned to the milestone and closed, and #19 was left open with a clarifying comment because it tracks automatic local-only / Not Owned metadata rather than user-defined custom tags. Validation: GitHub state verified with `gh pr view`, `gh issue view`, and `gh api` milestone checks.
- Implemented storage-backed local-only/not-owned source filtering. `ProductListQuery` now accepts `owned` and `localOnly` source groups, search terms such as `local` / `not owned` match local-only rows, bulk download requests use the same source filter, and the Library UI shows a `Local Only` system chip plus Source filter controls. Updated the unified product-list design note and local TODO trackers; public PR/issue tracking is intentionally deferred until user confirmation. Tightened two `dm-library` assertions to locate downloaded rows by work ID instead of assuming sort order. Validation: `cargo fmt --all`, targeted `cargo test -p dm-storage product_list_filters_and_searches_local_only_source`, `cargo test -p dm-storage`, `cargo test -p dm-library`, `cargo test -p dlsite-manager`, `cargo check -p dlsite-manager`, `cargo check --workspace`, `pnpm check`, `pnpm build`, and `git diff --check` passed.
- Fixed optional DLsite serial-page probing for non-serial direct downloads that now return HTTP 404 from `/home/serial/...`. `dm-api` now treats 404 like the previously observed 500/3xx absent-serial cases, so direct download planning can continue to the normal stream URL. Validation: `cargo fmt --all`, `cargo test -p dm-api detects_absent_optional_serial_page_statuses`, and `DMSITE_API_TEST_LIVE=0 cargo test -p dm-api` passed. A live env-enabled `cargo test -p dm-api` also surfaced separate API drift for the pinned serial-required fixture: `/home/serial/=/product_id/VJ01001165.html` returned 302 where the live test expects a parseable serial page.
- Set the initial document background in `src/app.html` to the app dark base color before Svelte component CSS loads, reducing white first-paint flicker when opening a new Tauri window. Validation: `pnpm check` and `pnpm build` passed.
- Fixed Activity page list layout so sparse job/audit rows stay content-height instead of stretching to fill their scroll panels. Confirmed completed jobs remain in Activity intentionally as recent job history until the user clicks `Clear`; the Downloads page remains scoped to active download jobs only. Validation: `pnpm check` and `pnpm build` passed.
- Updated public v3 tracking for confirmed simplified product categories. Closed #21 with an implementation note and updated PR #37 so #21 is listed under confirmed functionality and `Resolves #21` instead of the deferred list. Validation: `gh issue view 21 --json number,state,title,url` and `gh pr view 37 --json body --jq .body | rg -n "#21|Deferred|Confirmed functionality|Resolves"` confirmed the GitHub state.
- Adjusted generated GitHub wording to avoid third-person owner phrasing. PR #37 now uses first-person/neutral confirmation wording, and the generated #18 closing comment says the behavior was confirmed in-app. Validation: `gh pr view 37 --json body --jq .body | rg -n "project owner|product owner|manually confirmed|Manual packaging|I have manually"` and `gh api repos/AcrylicShrimp/dlsite-manager/issues/18/comments --jq '.[] | select(.id == 4423259761) | .body'` confirmed the updated text.
- Scanned all 23 closed GitHub issues directly via the API for `project owner`, `product owner`, owner-confirmation, and `confirmed by` wording. The only remaining hits are #10 comments using `product owners` to mean people who own DLsite products, including one old user-authored discussion comment and one external reply, so no v3-generated tracking text needed changes. Validation: closed-issue body/comment API scan completed.
- Removed the verbose release-candidate/build-artifact paragraph from PR #37 so the PR body stays focused on implemented scope, confirmed functionality, deferrals, and related issues. Validation: `gh pr view 37 --json body --jq .body | rg -n "Release candidate|Tauri build produced|Manual packaging|3\\.0\\.0-rc\\.1"` now only matches the deferred-version heading.
- Updated final v3 PR/issue tracking. Closed #20 and moved it from deferred to confirmed/resolved in PR #37, while #19 remains open with an explicit deferral comment and is still listed as deferred in the PR. The public `v3 rewrite` milestone now only has #35 open until PR merge/release. Validation: `gh issue view 20 --json number,title,state,url`, `gh issue view 19 --json number,title,state,url,comments --jq '{number,title,state,url,latestComment: .comments[-1].body}'`, `gh pr view 37 --json body --jq .body | rg -n "#19|#20|Deferred|Confirmed functionality|Related issues|Resolves"`, and `gh issue list --milestone "v3 rewrite" --state open --json number,title,url --limit 100`.
- Prepared final `3.0.0` release metadata after PR #37 was merged. Bumped package, Tauri, Rust crate, and lockfile versions from `3.0.0-rc.1` to `3.0.0`; validated the merged release branch with `cargo fmt --all`, `cargo check --workspace`, `DMSITE_API_TEST_LIVE=0 DMSITE_DOWNLOAD_TEST_LIVE=0 cargo test --workspace`, `pnpm check`, `pnpm build`, and `pnpm tauri build`. The Tauri build produced `target/release/bundle/macos/dlsite-manager.app` and `target/release/bundle/dmg/dlsite-manager_3.0.0_aarch64.dmg`.
