[package]
name = "dm-archive"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
thiserror = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
