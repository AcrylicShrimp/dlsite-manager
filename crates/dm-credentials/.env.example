# Optional live keychain test. This writes one temporary password to the native
# OS credential store, verifies it can be read back, then deletes it.
DMSITE_CREDENTIALS_TEST_KEYRING=0

# Optional service name override for the live keychain test.
DMSITE_CREDENTIALS_TEST_SERVICE=dlsite-manager-test
